import 'package:flutter/material.dart';

class Newslist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('NewslistPage'),
    );
  }
}
