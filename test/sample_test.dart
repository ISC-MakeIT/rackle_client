/// テスト用のフレームワーク
/// import 'package:flutter_test/flutter_test.dart';

/// envを読み取るClassとmethodを提供してくれる標準ライブラリ
/// import 'dart:io';

/// import 'package:rackle_client/lib/Models/models.dart';
/// import 'package:rackle_client/lib/API/RackleAPIClient.dart';

/// void main() {
	/// Platform.envirionment;
	/// test('hogehoge Model test', () async {
	///   final api = RackleAPIClient(env['URL']);
	///   final resp = api.getRequest(env['MODELNAME_PATH']));
	///   final parsed = Model.from(resp);
	///    
	///   expect(parsed, equal('横浜駅');
	///	});
///}

